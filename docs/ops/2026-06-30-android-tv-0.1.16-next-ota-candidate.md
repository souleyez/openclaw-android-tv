# OpenClaw Android TV 0.1.16 Next OTA Candidate

Date: 2026-06-30

## Purpose

`0.1.16 / 2026070102` is the next signed APK candidate after the current `0.1.15` one-device canary. It includes source changes through `480af32`, including APK-side install attempt status reporting for `installing`, `prompt_shown`, and recoverable `install_failed` outcomes.

This candidate is not yet published as the active `home` OTA release. The current production canary remains `0.1.15 / 2026070101` until the target device report is closed or the operator intentionally starts a new one-device release.

## Artifact

Local APK:

```text
C:\Users\soulzyn\Desktop\openclaw-tv-installers\OpenClawTV-0.1.16.apk
```

Source build artifact:

```text
C:\Users\soulzyn\Desktop\openclaw-android-tv\artifacts\android-tv\factory-pilot\openclaw-tv-0.1.16-2026070102-3128-platform-release-signed-20260630-194952.apk
```

SHA-256:

```text
2206353e7f653a52ecaab125c91b761b144d275ee2deccda6d4b59c7133385ac
```

Size:

```text
12124055
```

Package:

```text
com.openclaw.tv
```

Version:

```text
versionName=0.1.16
versionCode=2026070102
```

Signing certificate SHA-256:

```text
2d370c21f5dfd553d2a796314b70925fb38adeef90864c920bbbbb12887d3522
```

Signature verification:

```text
apksigner verify: v1=true, v2=true, v3=true
```

## Validation

Local validation completed:

```powershell
scripts\android-tv-build-factory-pilot.ps1 -Release
.\gradlew.bat :app:assembleRelease --console=plain --rerun-tasks
apksigner sign --key artifacts\signing\3128-platform\platform.pk8 --cert artifacts\signing\3128-platform\platform.x509.pem --v1-signing-enabled true --v2-signing-enabled true --v3-signing-enabled true
apksigner verify --verbose --print-certs C:\Users\soulzyn\Desktop\openclaw-tv-installers\OpenClawTV-0.1.16.apk
```

Results:

```text
Gradle release build -> BUILD SUCCESSFUL
3128 platform signing -> PASS
certSha256=2d370c21f5dfd553d2a796314b70925fb38adeef90864c920bbbbb12887d3522
v1=true
v2=true
v3=true
```

## Release Boundary

Use this candidate only as a next one-device OTA or recovery candidate after an operator decision. Do not treat it as evidence that the current `0.1.15` canary has installed. The factory pilot gate remains pending until a real target device report, factory fresh-machine feedback, vendor permission feedback, ADB/runtime evidence, and readiness-ledger rows close.
