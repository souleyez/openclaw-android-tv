# OpenClaw Android TV 0.1.15 OTA Candidate

Date: 2026-06-30

## Purpose

`0.1.15 / 2026070101` is a no-op OTA candidate built from the `0.1.14` factory baseline plus a version bump only. Its purpose is to prove the remote OTA path from `home` without introducing new functional risk.

## Artifact

Local APK:

```text
C:\Users\soulzyn\Desktop\openclaw-tv-installers\OpenClawTV-0.1.15.apk
```

SHA-256:

```text
9b007e2c90dde18d8f63e4a5f7415aef97a3cd377c00f2f355854ef833feab86
```

Size:

```text
12119959
```

Package:

```text
com.openclaw.tv
```

Version:

```text
versionName=0.1.15
versionCode=2026070101
```

Signing certificate SHA-256:

```text
2d370c21f5dfd553d2a796314b70925fb38adeef90864c920bbbbb12887d3522
```

Signature verification:

```text
apksigner verify: v1=true, v2=true, v3=true
```

## Suggested home OTA Release Fields

```text
projectKey=openclaw-android-tv
versionName=0.1.15
versionCode=2026070101
updateMode=full_apk
releaseChannel=factory-pilot
targetScope=deviceUuid:6741af4b-02b9-4692-99f3-5b4380fbbc3e
installPolicy=vendor_silent
artifactSha256=9b007e2c90dde18d8f63e4a5f7415aef97a3cd377c00f2f355854ef833feab86
artifactSize=12119959
artifactUrl=https://oc.goods-editor.com/storage/ota/openclaw-android-tv/OpenClawTV-0.1.15.apk
```

## Production home Release

Created on `oc.goods-editor.com` through the home platform API:

```text
releaseId=ota_openclaw-android-tv_2026070101_1782780116232_67ce5c33
rolloutStatus=rolling
rolloutPercent=100
deviceCount=1
minCheckIntervalSeconds=600
```

Public OTA URL verification:

```text
GET /storage/ota/openclaw-android-tv/OpenClawTV-0.1.15.apk -> 200
Content-Length=12119959
```

Bootstrap verification:

```text
target deviceUuid 6741af4b-02b9-4692-99f3-5b4380fbbc3e -> ota.available=true
non-target deviceUuid not-factory-pilot-device -> ota.available=false
```

## Validation

Local validation completed:

```powershell
.\gradlew.bat :app:testDebugUnitTest --console=plain
.\gradlew.bat :app:assembleRelease --rerun-tasks --console=plain
```

Both commands completed with `BUILD SUCCESSFUL`.

## Next Required Gate

Do not mark the OTA milestone complete until one test device installs this release from the remote manifest path instead of local ADB and reports the OTA lifecycle back to `home`.
